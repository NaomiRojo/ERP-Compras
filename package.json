{
  "name": "erp-final",
  "version": "0.1.0",
  "scripts": {
    "postinstall": "bun2nix -o bun.nix",
    "docker:migrate": "docker compose run --rm backend sh -lc \"bun install && bun run db:migrate\""
  },
  "module": "index.ts",
  "type": "module",
  "private": true,
  "workspaces": ["packages/*"],
  "devDependencies": {
    "@types/bun": "^1.3.2",
    "@typescript-eslint/parser": "^8.60.0",
    "bun2nix": "^2.0.0",
    "eslint": "^10.4.0"
  },
  "peerDependencies": {
    "typescript": "^5.9.3"
  }
}
